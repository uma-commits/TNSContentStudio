OPENROUTER_API_KEY=
ADMIN_PASSWORD_HASH=
DATA_DIR=./data

# Optional — only needed for personas using the HeyGen avatar engine.
HEYGEN_API_KEY=

# GOOGLE_API_KEY also powers the app's own image step (Nano Banana 2 Lite,
# lib/providers/nanobanana.ts) — free under Google AI Studio's daily quota,
# then ~$0.034/image. Falls back to Pollinations automatically if unset.

# /generate skill — image/video model routing (see .claude/skills/generate/SKILL.md)
KIE_API_KEY=
FAL_KEY=
WAVESPEED_API_KEY=
GOOGLE_API_KEY=
HF_API_KEY=
